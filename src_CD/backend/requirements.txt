# ChaseHound Backend - Minimal Bootstrap Requirements
# Le projet principal sera cloné depuis GitHub avec ses propres dépendances

# Core web framework pour le bootstrap
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
gunicorn==21.2.0

# Git pour cloner le repository
# (git est déjà installé sur App Engine)

# Utilities pour le bootstrap
requests==2.31.0 